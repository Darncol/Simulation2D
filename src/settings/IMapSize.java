package settings;

/**
 * The IMapSize interface defines the standard dimensions for a map.
 * Implementing classes can use these dimensions to create, modify,
 * or render a 2D map grid.
 */
public interface IMapSize {
    int MAP_WIDTH = 30;
    int MAP_HEIGHT = 10;
}
